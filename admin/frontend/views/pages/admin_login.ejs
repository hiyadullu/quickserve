<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin LogIn</title>
</head>
<style>
    * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', sans-serif;
}

body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background: #ffffff;
}

.container {
    display: flex;
    width: 100%;
    max-width: 1200px;
    height: 90vh;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0px 5px 20px rgba(0, 0, 0, 0.2);
}

/* Left Side (Images) */
.left-section {
    
    flex: 1.2;
    background: url('pinterest-bg.jpg') no-repeat center;
    background-size: cover;
    position: relative;
}

.overlay-text {
    position: absolute;
    bottom: 10%;
    left: 10%;
    width: 80%;
    font-size: 32px;
    color: white;
    font-weight: bold;
}

/* Right Side (Signup Form) */
.right-section {
    flex: 0.8;
    background: white;
    padding: 40px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
}

h2 {
    font-size: 24px;
    font-weight: bold;
    color: #333;
}

p {
    font-size: 14px;
    color: #666;
    margin-bottom: 20px;
}

form {
    display: flex;
    flex-direction: column;
}

label {
    text-align: left;
    font-size: 14px;
    margin-bottom: 5px;
    font-weight: 600;
    color: #444;
}

input {
    padding: 12px;
    margin-bottom: 15px;
    border: 1px solid #ddd;
    border-radius: 6px;
    font-size: 16px;
}

.continue-btn {
    background: #a6a107;
    color: white;
    border: none;
    padding: 12px;
    font-size: 16px;
    cursor: pointer;
    border-radius: 6px;
    font-weight: bold;
}

.or-divider {
    margin: 15px 0;
    font-size: 14px;
    font-weight: bold;
    color: #666;
}

.google-btn {
    background: white;
    border: 1px solid #ddd;
    padding: 12px;
    font-size: 16px;
    font-weight: bold;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    cursor: pointer;
    border-radius: 6px;
}

.google-btn img {
    width: 20px;
}

.terms-text {
    font-size: 12px;
    color: #666;
    margin-top: 15px;
}

a {
    color: #E60023;
    text-decoration: none;
}

</style>
<body>
    
    <div class="right-section">
        <h2>Welcome to QuickServe {admin Login}</h2>
        <p>Elegance served quickly</p>
        <form action="/auth/login" method="POST" id="loginForm">
            <label for="id">Admin ID:</label>
            <input type="id" name="admin_id" placeholder="Admin ID" required>
            
            <label for="password">Password</label>
            <input type="password" name="password" id="password" placeholder="Enter the Password" required>

            <button type="submit" class="continue-btn">Continue</button>
        </form>
        <p id="error-message" style="color: red;"></p>
    </div>
    
</body>
</html>